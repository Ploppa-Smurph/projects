// using ReactDom to 'render' the component to the DOM -- it is placing this in the HTML 'id="root" / or #root'
ReactDOM.render(<h1>This is my reactive statement</h1>, document.getElementById("root"));
